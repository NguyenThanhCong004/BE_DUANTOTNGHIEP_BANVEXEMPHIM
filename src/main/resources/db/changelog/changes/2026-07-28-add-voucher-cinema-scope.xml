<?xml version="1.1" encoding="UTF-8" standalone="no"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!-- Voucher giờ gắn với đúng 1 rạp: Admin tạo voucher tự động gán rạp của mình,
         Super Admin bắt buộc chọn 1 rạp (không có khái niệm voucher toàn hệ thống).
         Khách hàng chỉ áp dụng được voucher đúng rạp đang đặt vé — xem VoucherController,
         TicketCheckoutService. Backfill voucher cũ (dữ liệu dev/test) về rạp đầu tiên;
         môi trường mới triển khai chưa có voucher nào nên block backfill không ảnh hưởng. -->

    <changeSet author="movizone" id="2026-07-28-7-add-voucher-cinema-scope">
        <sql dbms="mssql">
            ALTER TABLE vouchers ADD cinema_id INT NULL;
            UPDATE vouchers SET cinema_id = (SELECT MIN(cinema_id) FROM cinemas) WHERE cinema_id IS NULL;
            ALTER TABLE vouchers ALTER COLUMN cinema_id INT NOT NULL;
            ALTER TABLE vouchers ADD CONSTRAINT FK_vouchers_cinema FOREIGN KEY (cinema_id) REFERENCES cinemas(cinema_id);
            CREATE INDEX ix_vouchers_cinema_id ON vouchers(cinema_id);
        </sql>
    </changeSet>

</databaseChangeLog>
