<?xml version="1.1" encoding="UTF-8" standalone="no"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!-- Snapshot ten ghe (VD "A1") tren ve, tach khoi bang seats. Cho phep admin xoa ghe
         khong con suat chieu hien tai/tuong lai ma khong lam mat ten ghe tren hoa don cua
         cac ve cu (showtime da qua) - seat_id se duoc gan NULL nhung seat_label van giu nguyen. -->
    <changeSet id="2026-08-25-add-ticket-seat-label-1" author="claude">
        <addColumn tableName="tickets">
            <column name="seat_label" type="nvarchar(20)"/>
        </addColumn>
    </changeSet>

    <changeSet id="2026-08-25-add-ticket-seat-label-2" author="claude">
        <sql dbms="mssql">
            UPDATE t SET t.seat_label = COALESCE(s.row, '') + COALESCE(s.number, '')
            FROM tickets t
            JOIN seats s ON s.seat_id = t.seat_id
            WHERE t.seat_id IS NOT NULL AND t.seat_label IS NULL;
        </sql>
        <rollback>
            <sql dbms="mssql">UPDATE tickets SET seat_label = NULL;</sql>
        </rollback>
    </changeSet>

</databaseChangeLog>
